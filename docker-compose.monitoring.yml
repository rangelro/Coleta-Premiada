# ═══════════════════════════════════════════════════════════════════════════════
#  Monitoramento agora é CENTRALIZADO em ../coleta-observability.
#
#  Para subir o stack de monitoramento:
#    cd ../coleta-observability && docker compose up -d
#
#  Os exporters específicos de cada projeto (postgres-exporter, mongodb-exporter)
#  já estão declarados nos docker-compose principais e se conectam à rede
#  coleta-observability automaticamente.
#
#  Este arquivo é mantido apenas para referência. Use 'make monitoring-up'
#  que agora aponta para o compose central.
# ═══════════════════════════════════════════════════════════════════════════════

# Para referência, os exporters que cada projeto expõe:
#
# Coleta-Premiada/docker-compose.yml:
#   - postgres-exporter:9187   (métricas PostgreSQL + queries customizadas)
#
# cp-collection-ms/docker-compose.yml:
#   - mongodb-exporter:9216    (métricas MongoDB + queries customizadas)
#
# coleta-premiada-frontend/docker-compose.yml:
#   - /metrics via prom-client  (métricas Next.js)
